{
  "buildCommand": "NODE_OPTIONS=--openssl-legacy-provider yarn build && NODE_OPTIONS=--openssl-legacy-provider yarn build:example",
  "outputDirectory": "dist",
  "framework": null,
  "build": {
    "env": {
      "CYPRESS_INSTALL_BINARY": "0"
    }
  }
}
