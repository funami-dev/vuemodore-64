module.exports = {
  // The example magazine is deployed next to Storybook, under /example/ of the
  // same site, so its assets have to resolve from there rather than the root.
  publicPath: '/example/',
};
